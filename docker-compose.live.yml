# Override for live trading (real orders). Use with:
#   docker compose -f docker-compose.yml -f docker-compose.live.yml up -d
services:
  topstepbot:
    command: ["trade", "--strategy", "${STRATEGY:-SampleStrategy}", "--live"]
